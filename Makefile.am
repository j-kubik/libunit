#libunitincludedir is exported from configure.ac - we need it so that .pc file can
# have the value too
libunitinclude_HEADERS = include/factors.h include/quantity.h \
                         include/SI.h      include/units.h

pkgconfigdir = $(libdir)/pkgconfig
nodist_pkgconfig_DATA = libunit.pc
